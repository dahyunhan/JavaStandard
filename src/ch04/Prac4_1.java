package ch04;

public class Prac4_1 {
    /*
            1. x > 10 && x < 20
            2. ch != " " || ch != "\t"
            3. ch == 'x' || ch == 'X'
            4. ch >= 0 && ch <=9
            5. ch >= 'A' && ch <='z'
            6. year % 400 == 0 && year % 4 == 0 && year % 100 != 0
            7. !powerOn
            8. str.equals("yes")
     */
}
